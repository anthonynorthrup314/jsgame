{
    "configurations": [
        {
            "name": "Launch Chrome against localhost",
            "request": "launch",
            "sourceMaps": true,
            "type": "chrome",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceRoot}"
        }
    ],
    "version": "0.2.0"
}